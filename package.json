{
  "name": "billingsaver-backend",
  "version": "1.0.0",
  "description": "BillingSaver backend - Mailchimp contact cleanup SaaS",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "nodemon src/index.js",
    "preinstall": "echo 'npm version:' && npm --version && echo 'node version:' && node --version && ls -la package.json"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.39.0",
    "axios": "^1.6.5",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "express-rate-limit": "^7.1.5",
    "node-cron": "^3.0.3",
    "stripe": "^14.10.0",
    "ws": "^8.21.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  },
  "engines": {
    "node": ">=18.0.0 <21.0.0"
  }
}
